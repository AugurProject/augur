var constants = require('../constants');
var utilities = require('../utilities');

var LogActions = {

  updateLog: function (message, type) {

    // add message and type to log
  }
};

module.exports = LogActions;
