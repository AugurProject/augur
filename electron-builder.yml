---
files:
- "!certs/*"
extraResources:
  - "**/*"
mac:
  asar: false
  target:
  - default
  icon: build/icon.png
win:
  target:
  - nsis
  icon: build/icon.png
linux:
  target:
  - deb
  - AppImage
  category: Network
appId: "net.augur.augur"
artifactName: "${os}-${productName}-${version}.${ext}"
publish:
  provider: github
