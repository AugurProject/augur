  {{#inputs}}
  let {{name}} = {{#GetTemplateForType}}{{name}}{{/GetTemplateForType}};
  {{/inputs}}

  let id = {{#GenerateIdStringTemplate}}{{/GenerateIdStringTemplate}};

  let entity = {{name}}Entity.load(id);
  if (entity == null) {
    entity = new {{name}}Entity(id);
  }
