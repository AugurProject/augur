import { AbstractDB } from './AbstractDB';

export class Markets extends AbstractDB {
    constructor() {
        super("Markets");
    }

    // Methods specific to updating the market DB and querying the market DB
}
