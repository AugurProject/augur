import { AbstractDB } from './AbstractDB';

export class UserShareBalances extends AbstractDB {
    constructor() {
        super("UserShareBalances");
    }

    // Methods specific to updating the UserShareBalances DB and querying the UserShareBalances DB
}
