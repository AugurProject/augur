import React, { Component } from "react";
import PropTypes from "prop-types";
import classNames from "classnames";

const ReportingReport = p => (
  <section>
    Report
  </section>
);

export default ReportingReport;
