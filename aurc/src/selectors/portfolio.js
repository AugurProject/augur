import navItems from 'selectors/portfolio-nav-items';
import totals from 'selectors/portfolio-totals';
import positions from 'selectors/login-account-positions';
import markets from 'selectors/login-account-markets';
import reports from 'selectors/login-account-reports';

export default {
	navItems,
	totals,
	positions,
	markets,
	reports
};
