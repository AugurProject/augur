import { assert } from 'chai';

describe(`modules/auth/reducers/login-account.js`, () => {
  it(`should logout of an account`);
  it(`should updated the logged in account`);
  it(`should clear the logged in account`);
});
