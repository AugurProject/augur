import { assert } from 'chai';

describe(`modules/auth/selectors/auth-form.js`, () => {
  it(`should select the correct auth form`);
  it(`should error with incorrect user`);
  it(`should error with incorrect password`);
  it(`should error with too short password`);
  it(`should error with passwords that don't match`);
});
