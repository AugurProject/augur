import { assert } from 'chai';

describe(`modules/market/selectors/market.js`, () => {
  it(`should return the selected market id`);
  it(`should be able to select a market`);
  it(`should be able to select a market by event id`);
  it(`should be able to assemble a market`);
});
