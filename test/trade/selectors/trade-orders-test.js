import {
	assert
} from 'chai';
// import {
//   formatShares,
//   formatEther,
//   formatNumber
// } from '../../../src/utils/format-number';
// import * as selector from '../../../src/modules/trade/selectors/trade-orders';

describe(`modules/trade/selectors/trade-orders.js`, () => {
	let out, test;
	// let market = {
	//   outcomes: [{
	//     id: 'test1',
	//     name: 'testOutcome1'
	//   }, {
	//     id: 'test2',
	//     name: 'testOutcome2'
	//   }]
	// };
	// let makeTradeInProgress = {
	//   test1: {
	//     numShares: 10,
	//     totalCost: 100,
	//   },
	//   test2: {
	//     numShares: 5,
	//     totalCost: 50,
	//   }
	// };
	// let dispatch = (item) => item;

	// test = selector.selectTradeOrders(market, makeTradeInProgress, dispatch);
	// console.log(test);

	it(`[Not in use?] should select the trade orders`);
});
