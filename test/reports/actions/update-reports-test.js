import {assert} from 'chai';

describe(`modules/reports/actions/update-reports.js`, () => {
  it(`should load reports given marketdata`);
  it(`should return a update reports action`);
  it(`should return a clear reports action`);
});
