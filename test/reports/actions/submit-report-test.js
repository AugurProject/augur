import {assert} from 'chai';

describe(`modules/reports/actions/submit-report.js`, () => {
  it(`should return a function that submits a report`);
  it(`should return a function that processes a report`);
});
