import {
  assert
} from 'chai';

describe(`modules/transactions/actions/add-transaction.js`, () => {
  it(`should add transactions`);
  it(`should add a transaction`);
  it(`should make a transaction id`);
});
