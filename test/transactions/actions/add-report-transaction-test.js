import { assert } from 'chai';

describe(`modules/transactions/actions/add-report-transaction.js`, () => {
  it(`should return a function to add a report transaction`);
  it(`should return an object to make a report transaction`);
});
