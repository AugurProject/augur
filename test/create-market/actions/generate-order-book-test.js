import {
    assert
} from 'chai';

describe('modules/create-market/actions/generate-order-book.js', () => {
    it('[TODO] should be able to submit a request to generate an order book');
    it('[TODO] should be able to generate an order book');
});