import { assert } from 'chai';

describe(`modules/markets/actions/load-markets.js`, () => {
  it(`should dispatch an update markets action with updated market data from AugurJS`);
  it(`should dispatch a load reports action.`);
});
