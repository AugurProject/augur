import { assert } from 'chai';

describe(`modules/markets/actions/update-selected-page-num.js`, () => {
  it(`should return a function to dispatch an UPDATED_SELECTED_PAGE_NUM action and scroll the window.`);
});
