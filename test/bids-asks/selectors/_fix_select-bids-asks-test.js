import { assert } from 'chai';

describe(`modules/bids-asks/selectors/_fix_select-bids-asks.js`, () => {
  it(`should select the outcome Bids`);
  it(`should select the outcome Asks`);
  it(`should select the Bids Asks by Price`);
  it(`should select the Outcome Bids Asks`);
});
