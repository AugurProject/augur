import React from 'react';

const CreateMarketProgress = p => (
  <article className="create-market-progress">
    <span>Create Market Progress -- {p.currentStep}</span>
  </article>
);

export default CreateMarketProgress;
