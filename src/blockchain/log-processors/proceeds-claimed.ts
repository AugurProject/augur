import { Database } from "sqlite3";
import { FormattedLog, ErrorCallback } from "../../types";

export function processProceedsClaimedLog(db: Database, log: FormattedLog, callback: ErrorCallback): void {

}
