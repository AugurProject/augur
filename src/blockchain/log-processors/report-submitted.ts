import { Database } from "sqlite3";
import { FormattedLog, ErrorCallback } from "../../types";

export function processReportSubmittedLog(db: Database, log: FormattedLog, callback: ErrorCallback): void {

}
