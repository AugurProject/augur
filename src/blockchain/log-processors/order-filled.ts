import { Database } from "sqlite3";
import { FormattedLog, ErrorCallback } from "../../types";

export function processOrderFilledLog(db: Database, log: FormattedLog, callback: ErrorCallback): void {

}
