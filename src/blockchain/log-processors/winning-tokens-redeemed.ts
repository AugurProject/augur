import * as Knex from "knex";
import { FormattedLog, ErrorCallback } from "../../types";

export function processWinningTokensRedeemedLog(db: Knex, trx: Knex.Transaction, log: FormattedLog, callback: ErrorCallback): void {

}
