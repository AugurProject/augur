import * as Knex from "knex";
import { FormattedLog, ErrorCallback } from "../../types";

export function processReporterRegisteredLog(db: Knex, log: FormattedLog, callback: ErrorCallback): void {

}
